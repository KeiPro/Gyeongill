20.05.18 오전수업.
1. enemy.h와 cpp수정.
2. #define으로 매크로 생성
	#define SAFE_DELETE(p) { if(p) { delete p; p = NULL; } }
	#define SAFE_ARR_DELETE ~~~~
3. enemy 10개 동적할당 생성

4. 디버그 -> 창 -> 진단도구 표시
   ( 스냅샷 만들기 (만약 이게 없으면 힙 프로파일링 클릭) )
	--> 똑같은 이미지를 가진 녀석을 다 따로따로 생성해주기 보다 
	     하나의 주소를 가지고 복사해서 사용하는것이 효율적이다.

5. 새로운 자료구조 map등장. --> 배열로 할 경우 0, 1, 2, 3, ... 번호로 어떤 정보를 알기에는
	헷갈리므로 key와 value쌍으로 묶여있는 자료구조를 이용해야 한다.

iterator : 키와 밸류가 페어로 묶어서 저장되어있는 메모리 주소.. 결국엔 얘도 포인터다.

e누르면 아래로 하나 하나 추가되게.